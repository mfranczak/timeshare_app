<h1>Activities#index</h1>
<p><a href="<%= new_activity_path %>">Add+</a></p>

<% @activities.each do |day, activity| %>
    <%= render partial: 'daily', locals: {day: day, activity: activity} %>
<% end %>